
module.exports = Object.freeze({
    MIN_PLAYERS_NEEDED: 2,
    MAX_PLAYERS_NEEDED: 0, // 0 = unlimited
    TIME_TO_DRAW: 15, // seconds
    DRAWING_DELAY: 5
});

//define("PI", 3.14);
//define("MIN_PLAYERS_NEEDED",2);
//const TIME_TO_DRAW = 60; // seconds
//const MAX_PLAYERS = 0; // 0 = unlimited
